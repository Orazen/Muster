node_modules
.worktrees/
dist
dist-electron
dist-native
# the sidecar's compiled entry — regenerated by `pnpm build:companion`,
# which package:prepare runs before electron-builder stages it
dist-companion
dist-server
*.local
.DS_Store
*.tsbuildinfo
electron/resources/speech-helper
electron/resources/Muster Speech.app
release
.omb-scratch
.wrangler/
.dev.vars
.dev.vars.*
cloudflare/composio-broker/worker-configuration.d.ts
.claude/worktrees/
testsprite_tests/TC*.py
testsprite_tests/tmp/

# android-companion is npm-installed (outside the pnpm workspace); keep its
# modules out of the repo; commit the standalone lockfile for reproducible tests
android-companion/node_modules/

# Xcode: the .xcodeproj is generated (see ios/README.md), and xcodebuild
# writes DerivedData-style output into ios/build when invoked from CLI
ios/build/

# Muster+ additions - prevent these from being committed
.claude-flow/
.claude/
musterbot-background/
commit-audit-fixes.sh
.DS_Store
*.log
node_modules/
dist/
.env
.env.local

# Claude AI tool artifacts (Muster+ additions)
.claude/
.claude-flow/
musterbot-background/
commit-audit-fixes.sh

# agent tool-state directories — never product content
.mimosa/
.v2c/
.video_agent/
